# Swift / SPM
.build/
.swiftpm/
*.xcodeproj/
xcuserdata/
DerivedData/

# macOS
.DS_Store
._*

# Icon generation artifacts
*.iconset/

# IDE
.vscode/
.idea/

# Claude / local agent tooling state — session transcripts and memory, never publish
.claude/
.claude-flow/
.serena/
CLAUDE.md

# Planning & internal docs
ROADMAP.md
HANDOFF.md

# Antigravity's OAuth client — a third party's credential, copied into the bundle by build.sh.
# Keep it out of the repo: a public copy is what gets such a client rotated. See the .example.
packaging/antigravity-client.json

# Release artifacts (built locally and attached to GitHub releases, never versioned)
*.tar.gz
*.dmg
*.zip

# Build artifacts — regenerated by build.sh, never commit
Fluxa.app/

# Icon generation script — utility, run manually when needed
generate_icon.swift
